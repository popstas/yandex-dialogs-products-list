encoding "utf8";

TTextMinerConfig {
  Dictionary = "mydic.gzt"; // путь к корневому словарю

  PrettyOutput = "PrettyOutput.html"; // путь к файлу с отладочным выводом в удобном для чтения виде

  // Input = {File = "input2.txt"}
  // Output = { File = "output.txt" }

  Articles = [
    { Name = "ЧтоГде" } // название статьи в корневом словаре, которая содержит запускаемую грамматику
  ]

  Facts = [
    { Name = "WhatWhere" },
    { Name = "Test" }
  ]
}